import React from 'react';
import styles from './Profile.module.scss'

const Profile = (props) => {
    return (
        <main>
            <h1>Profile</h1>
        </main>
    );
}

export default Profile; 