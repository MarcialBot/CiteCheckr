import React from 'react';
import styles from './Home.module.scss';

const Home = (props) =>  {
    return (
        <main>
            <h1>Homepage</h1>
        </main>
    );
}

export default Home;